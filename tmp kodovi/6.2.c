#include <stdio.h>

int condition(int x, int y){
    
    aa;
    return !(!x && !y) ? y : x;

    assa;
    sasd rsas ? asdasda ds das sa  : dasda>asdasd;
}

int main(){

    int x = condition(0, 0);
    printf("%d\n", x);

    return 0;
}